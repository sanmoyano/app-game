import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
    margin: 20,
    alignItems: "center",
    padding: 20,
    borderRadius: 12,
  },
});
